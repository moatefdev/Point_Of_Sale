export class ApiConfig {
  static serverUrl = 'https://pos-api-nodejs-website.herokuapp.com/';
  static userRoute = `${ApiConfig.serverUrl}users/`;
  static productRoute = `${ApiConfig.serverUrl}products/`;
  static cateogryRoute = `${ApiConfig.serverUrl}category/`;
}
